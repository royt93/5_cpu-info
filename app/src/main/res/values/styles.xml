<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="AppThemeBase" parent="@style/Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primaryDark</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="colorOnPrimary">@color/onPrimary</item>
        <item name="colorOnSecondary">@color/onPrimary</item>
        <item name="colorAccent">@color/accent</item>
        <item name="colorError">@color/error</item>
        <item name="colorOnError">@color/onError</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/onSurface</item>
        <item name="android:colorBackground">@color/background</item>
        <item name="colorOnBackground">@color/onBackground</item>
        <!-- Use surface as window background so Activity.recreate() (e.g. locale change)
             doesn't show a contrasting flash between destroyed and new activity instances. -->
        <item name="android:windowBackground">?attr/colorSurface</item>
        <!-- android:statusBarColor/navigationBarColor removed — dead since targetSdk 35+
             (edge-to-edge enforced, system ignores both attrs). Real status/nav bar styling now
             lives in code via ComponentActivity.enableEdgeToEdge() (see util/Ext.kt,
             ActVip/SplashActivity) — see doc/task/epic-02-techdebt.md Story 11 T2.32/T2.34. -->
        <item name="alertDialogTheme">@style/CustomAppCompatAlertDialogStyle</item>
        <item name="materialAlertDialogTheme">@style/CustomAppCompatAlertDialogStyle</item>

    </style>

    <style name="AppThemeBase.Launcher">
        <!-- Suppress the "preview window" Android paints between activity instances +
             remove Activity transition animations so locale-change recreate happens
             without a fade-through-black frame. -->
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowAnimationStyle">@style/NoActivityAnimation</item>
        <item name="colorPrimaryDark">@color/background</item>
    </style>

    <style name="NoActivityAnimation" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@null</item>
        <item name="android:activityOpenExitAnimation">@null</item>
        <item name="android:activityCloseEnterAnimation">@null</item>
        <item name="android:activityCloseExitAnimation">@null</item>
    </style>

    <!-- Bottom sheet rounded corners + expanded state are applied at runtime via
         BaseRoundedBottomSheet (MaterialShapeDrawable) — kept as-is after the M2→M3 base theme
         switch (Story 11 T2.35): the app theme is M3 now, so a declarative M3 BottomSheet style
         would overlay cleanly, but the runtime approach isn't broken by anything, just not
         required anymore — not migrated in this pass to keep the diff scoped to theme/color. -->

    <style name="TintedProgressBar" parent="Theme.AppCompat.Dialog.Alert">
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="CustomAppCompatAlertDialogStyle" parent="ThemeOverlay.Material3.Dialog.Alert">
        <item name="colorPrimary">@color/onBackground</item>
    </style>

    <!-- Texts -->
    <style name="BaseText">
        <item name="android:textColor">?attr/colorOnBackground</item>
    </style>

    <style name="TextH6" parent="BaseText">
        <item name="android:textSize">20sp</item>
    </style>

    <style name="TextH7" parent="BaseText">
        <item name="android:textSize">18sp</item>
    </style>

    <style name="TextBody1" parent="BaseText">
        <item name="android:textSize">16sp</item>
    </style>

    <style name="TextBody2" parent="BaseText">
        <item name="android:textSize">14sp</item>
    </style>

    <style name="TextAnnotation1" parent="BaseText">
        <item name="android:textSize">12sp</item>
    </style>

    <!-- Overrides Material Components v1's TextAppearance.Design.Tab default (textAllCaps=true) —
         combined with a large system font scale, all-caps + the default bold letter-spacing made
         tab labels look disproportionately bulky next to sentence-case body text at the same sp
         size (real user report). NOTE: TextAppearance.Design.Tab sets the AppCompat custom
         "textAllCaps" item (no android: prefix), not the framework android:textAllCaps — must
         override the same unprefixed attribute or it silently has no effect. -->
    <style name="TabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
    </style>

    <style name="TextAnnotation2" parent="BaseText">
        <item name="android:textSize">10sp</item>
    </style>

    <style name="ShapeAppearance.App.Image.Circle" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
        <item name="android:background">#ffffffff</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowBackground">@drawable/startup</item>
    </style>
</resources>
