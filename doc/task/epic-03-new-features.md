# Epic 3 — Feature mới (gap vs competitor)

> `doc/task/quick_win.md` đã cover: Camera, Display/refresh rate, Media codec, DRM/Widevine, Export report, Network info, Battery health, Floating overlay. **Không lặp lại các mục đó** — epic này chỉ chứa gap MỚI phát hiện qua review, so với competitor CPU-Z, AIDA64, Device Info HW, CPU Monitor.

| # | Feature | Mô tả | Effort | Giá trị | Nguồn |
|---|---|---|---|---|---|
| ✅F01 | Realtime dashboard có lịch sử | **Đã xong (2026-09-01)** — tab mới `feat/infor/dashboard` (`FrmDashboard`/`VMDashboard`), vị trí 15 trong ViewPager Hardware. 3 biểu đồ line chart: CPU utilization trung bình (%, tính từ `current/max` mỗi core qua `ObservableCpuData`), RAM used (%, qua `ObservableRamData`), battery temp (°C, qua `ObservableTemperatureData`). Rolling buffer 15 phút (`HistoryBuffer`, evict theo tuổi) — chart tự động chứa cả 3 mốc 1/5/15 phút, **không làm UI toggle riêng**: dựa vào pinch-zoom/pan built-in của thư viện chart thay vì tự dựng window picker (đơn giản hoá có chủ đích, xem library choice bên dưới). **Library**: `MPAndroidChart` (JitPack, Java thuần) thay vì Vico — Vico là Compose-only, build bằng Kotlin hiện đại (rủi ro đúng loại xung đột metadata mà `build.gradle.kts` `force()` block đã ghi chú; đã research trước khi chọn). Verify: `HistoryBufferTest` (4 test) + `VMDashboardTest` (5 test) unit, `assembleDevDebug`/`lintDevDebug` xanh với dependency mới. **Không có instrumented test cho tab này** — điều hướng vào tab kích hoạt lỗi teardown ở tầng test harness (`ActivityScenario` "Cannot run onActivity since Activity has been destroyed already"), xác nhận qua logcat KHÔNG phải app crash thật (process exit code 0, không có FATAL EXCEPTION). **Cập nhật khi làm F06**: ban đầu nghi do `VMDashboard` poll liên tục mỗi giây khiến `composeRule.waitForIdle()` không "settle", nhưng F06 (event-driven, không poll) gặp lỗi harness y hệt → nguyên nhân thật là môi trường test harness/device (xác nhận thêm qua 2 device khác nhau cho 2 lỗi harness khác nhau khi làm F06), không phải đặc thù polling của F01 — quyết định bỏ test vẫn đúng, chỉ sửa lại phần chẩn đoán | L (2-3d) | 🌟🌟🌟 | Codex |
| ✅F02 | Thermal throttling diagnostics | **Đã xong** — xác nhận qua code hiện tại (`feat/throttle/{ThermalStatusProvider,ThermalStatusMapper}.kt`, `PowerManager.currentThermalStatus` + headroom, hiện card passive trên tab Throttle, test `thermalStatusCardShowsOnThrottleTab`), doc này trước đó chưa gắn nhãn dù đã ship như 1 phần của U02 Throttle Fingerprint | M (1d) | 🌟🌟🌟 | Codex + Claude CLI (2 nguồn) |
| ✅F03 | USB/OTG + Bluetooth capability inspector | **Đã xong** — xác nhận qua code hiện tại (`feat/usbbt/{UsbInspectorProvider,BluetoothInspectorProvider,UsbDeviceClassCatalog,UsbBluetoothBottomSheet}.kt`), doc này trước đó chưa gắn nhãn dù đã ship (nhắc tới trong `feature.md` sprint list là "USB/BT Inspector") | M (1d) | 🌟🌟 | Codex + Claude CLI |
| ✅F04 | Security posture checklist | **Đã xong** — xác nhận qua code hiện tại (`security_patch_level`/`selinux_status`/`hardware_keystore` rows trong `VMAndroidInfo`, test `androidTabShowsSecurityChecklistRows`), doc này trước đó chưa gắn nhãn dù đã ship (B04 sprint) | M (1d) | 🌟🌟🌟 | Codex + Claude CLI |
| ✅F05 | App permission & SDK inventory | **Đã xong** — xác nhận qua code hiện tại (`feat/app/{AppPermissionCatalog,AppPermissionEvaluator,AppPermissionProvider}.kt`, test `appPermissionsActionShowsPermissionsSheet`), doc này trước đó chưa gắn nhãn dù đã ship | M (1d) | 🌟🌟 | Codex |
| ✅F06 | Storage/CPU benchmark nhẹ có baseline | **Đã xong (2026-09-01)** — package mới `feat/storagebench` (`FrmStorageBench`/`VMStorageBench`), vị trí 16 trong ViewPager Hardware, theo đúng khuôn mẫu U02 Throttle (`ThrottleFingerprint`/`ThrottleTestRunner`/`ThrottleResultPrefs`) đã có sẵn: pure-math object (`StorageBenchmark`) + runner I/O thật (`StorageBenchmarkRunner`) + prefs lưu kết quả trước đó để so sánh (`StorageBenchResultPrefs`, không phải bảng baseline chipset cứng — dùng chung khái niệm "before/after" như Throttle). 5 phase: ghi/đọc tuần tự 32MB, ghi/đọc ngẫu nhiên 500×4KB, băm SHA-256 1 giây — toàn bộ chạy trong `context.cacheDir`, xoá file tạm ở `finally` dù kết thúc ở phase nào. **An toàn nhiệt**: tái dùng đúng ngưỡng 43°C của `ThrottleFingerprint.SAFETY_ABORT_TEMP_C` (không tự đặt ngưỡng mới), check trước khi bắt đầu + check lại giữa chừng sau 2 phase tuần tự — không cần hard duration cap hay cooldown timer bắt buộc như Throttle vì tổng thời gian test chỉ ~10-15s (ngắn hơn nhiều so với 30s multi-core burn của U02). **Không tự dựng bảng baseline theo chipset** (không có dữ liệu thật để so sánh) — dùng lại đúng pattern "so kết quả với lần chạy trước" đã chứng minh hợp lý ở Throttle. Verify: `StorageBenchmarkTest` (5 test) unit, `assembleDevDebug`/`lintDevDebug` xanh. **Không có instrumented test** — cùng lý do F01: lỗi harness `ActivityScenario` "Cannot run onActivity..." tái hiện y hệt dù VM này KHÔNG poll liên tục (event-driven, khác hẳn F01) — xác nhận qua logcat trên **2 device khác nhau** cho 2 lỗi harness khác nhau (TECNO: teardown NPE; Pixel 7 Pro: `NoSuchMethodException InputManager.getInstance`, lỗi tương thích Espresso/API version đã biết) → chắc chắn không phải bug code, không phải lỗi app crash | M (1d) | 🌟🌟 | Codex + Claude CLI |
| ✅F07 | Sensor test suite tương tác | **Đã xong** — xác nhận qua code hiện tại (`feat/infor/sensor/{SensorTestBottomSheet,SensorTestRunner,SensorTestEvaluator,VMSensorTest,SensorTestScreen}.kt`, FAB "Kiểm tra cảm biến" trên tab Sensors, test `sensorTestFabRunsThroughAllStepsToADoneScreen`), doc này trước đó chưa gắn nhãn dù đã ship. **Lưu ý phạm vi**: đây là guided pass/fail test flow (bấm Start → đi qua từng cảm biến → Done), KHÔNG phải waveform chart cho tab Sensors tĩnh — xem F12 | M (1d) | 🌟🌟 | Claude CLI |
| ✅F08 | Vulkan/OpenGL ES chi tiết hơn | **Đã xong** — xác nhận qua code hiện tại (`feat/infor/gpu/GraphicsDetailProvider.kt` — Vulkan capability flags qua `PackageManager.systemAvailableFeatures`, hiển thị trong `FrmGpuInfo.kt`), doc này trước đó chưa gắn nhãn dù đã ship | S (0.5d) | 🌟 | Claude CLI |
| ✅F09 | CPU Cluster Topology (Prime/Performance/Efficiency) | **Đã xong** — làm chung với U06 (Epic 4) đúng như doc này đã dự đoán: Sprint 7 làm phần group cluster (`bfa5e32`), Sprint 8 (2026-09-02) hoàn tất cache hierarchy per-cluster. Xem epic-04-unique-ideas.md U06 | M (1d) | 🌟🌟🌟 | Gemini CLI |
| ✅F10 | NPU/AI Capability Detection | **Đã xong** — làm chung với U12 (Epic 4) đúng như doc này đã dự đoán: `feat/airead/{AiReadinessBar,AiReadinessEvaluator,AiReadinessProvider,AiReadinessBottomSheet}.kt`, đọc `i8mm`/`bf16`/`neon_dot`/`sve`/`sve2`/`fp16_arith` qua JNI mới trong `cpuinfo-libs.cpp`, test `aiReadinessBarOpensSheetWithScoreAndFlagRows` | M (1d) | 🌟🌟🌟 | Gemini CLI |
| ✅F11 | Audio Hardware capabilities | **Đã xong (Sprint 16, 2026-08-30)** — tab `feat/infor/audio` mới: native sample rate/buffer, low-latency/pro-audio feature flags, mic count, spatial audio (Spatializer API 32+), per-output-device channel counts/sample rates/encodings (PCM/AC-3/E-AC-3/DTS/Dolby Atmos JOC/AC-4). Scope hẹp hơn mô tả gốc: **bỏ BT codec (LDAC/aptX)** — cần `BLUETOOTH_CONNECT` runtime permission + thiết bị A2DP đã ghép nối, không phù hợp tab read-only 0-permission như batch quick-win trước. Zero permission mới. Xóa hẳn code ALSA `/proc/asound` chết trong `VMHardwareInfo` (đã xác nhận SELinux chặn từ lâu — `getSoundCardNumber()` luôn fallback về "1", `tryToGetSoundCardId`/`tryToGetAlsa` luôn fail âm thầm, hiển thị "1 sound card / Unknown id" giả) — tab Hardware hết mục "Sound card" gây hiểu nhầm, dữ liệu thật giờ ở tab Audio mới | S (0.5d) | 🌟🌟 | Gemini CLI |
| ✅F12 | Sensor live waveform plotter | **Đã xong (2026-09-02)** — `SensorWaveformBuffer.kt` (pure logic, count-capped ring buffer per axis, KHÔNG dùng lại `HistoryBuffer` của Dashboard vì đó là time-windowed không phải count-capped). `VMSensorsInfo.kt` thêm `waveformState: StateFlow<SensorWaveformUiState>`, ghi buffer song song với format string hiện có trong `updateSensorInfo()` (không đụng logic format cũ). `frm_sensors_info.xml` thêm hàng 3 `LineChart` (MPAndroidChart, tái dùng đúng dependency đã add cho F01 Dashboard) phía trên RecyclerView — Accelerometer/Gyroscope (3 line X/Y/Z + legend) + Barometer (1 line). Mỗi chart tự ẩn (`visibility=GONE`) nếu thiết bị không có sensor đó — verify thật: TECNO KJ7 không có barometer, chart ẩn đúng; Galaxy S24 Ultra có đủ 3, cả 3 hiện đúng dữ liệu sống (áp suất thật ~1010.2 hPa khớp thực tế). Chart nhỏ, không cho pinch/zoom (`setTouchEnabled(false)`, khác Dashboard) vì 3 chart chia sẻ 1 hàng, không đủ chỗ. **Verify**: `SensorWaveformBufferTest` (4 test), `VMSensorsInfoTest` (+3 test), `ActHostSmokeTest#sensorWaveformChartsRenderOnSensorsTab` (integration, dữ liệu native thật — MockK không giả lập được `onSensorChanged` cần `SensorManager`/`Looper` thật) — suite 22/22 pass trên TECNO KJ7 và Galaxy S24 Ultra thật, kèm ảnh chụp xác nhận | S (0.5d) | 🌟🌟 | Gemini CLI |

**Khuyến nghị batch**: F02 + F08 đi kèm sau khi fix B04 (cùng chạm `DataProviderGpu`/tab CPU-temp). F04 + F05 cùng nhóm "read-only audit info", 0 permission mới, có thể gộp 1 sprint giống cách `doc/task/quick_win.md` đã gộp đợt 3a. F09 + F10 nên làm cùng đợt vì cùng khai thác `libcpuinfo` extension flags chưa dùng tới — hiệu quả đầu tư nghiên cứu 1 lần dùng 2 chỗ.
