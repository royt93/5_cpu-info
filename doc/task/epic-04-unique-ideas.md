# Epic 4 — Tính năng độc quyền (USP / signature differentiator)

> Mục tiêu: tính năng chưa app nào trong phân khúc "system info" làm, tận dụng lợi thế kỹ thuật riêng (native `cpuinfo` lib đang under-used, hệ VIP có sẵn, `SystemInfoExporter` có sẵn khung).

## 🏆 Top pick (3 nguồn AI độc lập cùng tự đề xuất — tín hiệu rất mạnh)

### U01 — "Device Truth Score" / Chip authenticity check / "Silicon Detective"
Đối chiếu thông tin SoC thật từ native `cpuinfo` (microarchitecture, ISA, core topology, và sâu hơn: thanh ghi phần cứng `MIDR`/`MPIDR`/`REVIDR`/CPU Part number — đọc trực tiếp tầng Native, bỏ qua hoàn toàn chuỗi giả mạo trong `Build.MODEL`/`/proc/cpuinfo`) với `Build` fields, ABI, GPU/Vulkan, camera metadata → phát hiện firmware giả spec, chip dựng/re-marked, máy refurb/xách tay khai sai model, xác định silicon stepping/revision. Xuất bằng chứng từng điểm mismatch cụ thể, không chỉ 1 điểm số mơ hồ.
**Giá trị**: cực cao cho thị trường mua bán máy cũ — không competitor nào (CPU-Z, AIDA64) làm sâu tới mức "phát hiện giả mạo bằng thanh ghi phần cứng", họ chỉ hiển thị info bề mặt.
**Nguồn**: Codex (P0, "Device Truth Score") + Claude CLI ("Chip authenticity check") + Gemini CLI ("Silicon Detective — SoC Binning Verification") — **3 nguồn độc lập, không thấy bài nhau, cùng hội tụ về đúng 1 ý tưởng cốt lõi**.
**Effort**: L (cần nghiên cứu kỹ database chipset thật vs khai báo + đọc thanh ghi native).

### U02 — Throttling Fingerprint / Freq-vs-temp curve / "Hardware Degradation Audit"
Chạy workload ngắn có kiểm soát (Gemini CLI đề xuất cụ thể: stress test 60s đa luồng native), ghi xung từng cluster + thermal status + pin theo thời gian thực → "đường cong chịu tải" so sánh được trước/sau khi thay pin, update ROM, thay keo tản nhiệt. So sánh tỷ lệ suy giảm hiệu năng Cold state vs Throttled state, xuất "Hardware Health Card" đồ hoạ có thể chia sẻ (watermark VIP). Tận dụng infra `ObservableCpuData` polling đã có sẵn (Epic 2 Story 6, T2.18) + `feat/temp`.
**Giá trị**: chưa app nào trong phân khúc trực quan hoá tương quan freq-temp tốt, và "chứng nhận sức khỏe phần cứng" chia sẻ được là hook viral tự nhiên.
**Nguồn**: Codex ("Throttling Fingerprint") + Claude CLI ("Freq-vs-temp correlation live graph") + Gemini CLI ("VIP Hardware Degradation & Thermal Throttling Audit") — **3 nguồn độc lập cùng ý tưởng cốt lõi**.
**Effort**: M-L. Lịch sử dài hạn có thể là tính năng VIP-only (monetization hook tự nhiên).

### U12 — "AI Readiness Score" (mới, Gemini CLI)
Tổng hợp các cờ tập lệnh AI mà `libcpuinfo` đã có sẵn nhưng chưa khai thác (`cpuinfo_has_arm_i8mm`, `cpuinfo_has_arm_bf16`, `cpuinfo_has_arm_neon_dot`, `cpuinfo_has_arm_sve/sve2`) cùng RAM khả dụng + số core hiệu năng cao → chấm điểm "máy chạy được on-device LLM cỡ nào" (vd "Đạt chuẩn chạy LLM 3B" / "Đạt chuẩn xử lý ảnh GenAI"). Dùng chung nền tảng dữ liệu với F10 (Epic 3, NPU/AI Capability Detection).
**Giá trị**: đúng xu hướng on-device AI 2025-2026 (Gemini Nano và tương tự), chưa app system-info nào trên Play Store có chỉ số này — timing tốt.
**Nguồn**: Gemini CLI (1 nguồn, nhưng dữ liệu nền `libcpuinfo` cực kỳ cụ thể và đã sẵn có, effort MVP thấp vì F10 đã build data layer).
**Effort**: S-M nếu làm sau F10 (tái dùng data layer).

## Ý tưởng khác

| # | Ý tưởng | Mô tả ngắn | Nguồn |
|---|---|---|---|
| ✅U03 | Hardware Diff / Before-After Snapshot | **Đã xong (Sprint 9, commit `e2e96f2`)** — `feat/snapshot/{HardwareSnapshotProvider,HardwareSnapshotEvaluator,HardwareSnapshotBottomSheet}.kt`, entry point trong Settings. Doc này chưa được cập nhật lúc đó — xác nhận lại qua code + git log (2026-09-01), không phải việc mới | Codex |
| ✅U04 | Privacy-preserving Fleet Compare | **Đã xong (Sprint 14, commit `10bb213`)** — `feat/fleet/{FleetCompareProvider,FleetCompareEvaluator,FleetCompareBottomSheet,FleetSpecCatalog}.kt`. Doc này chưa được cập nhật lúc đó — xác nhận lại qua code + git log (2026-09-01), không phải việc mới | Codex |
| ✅U05 | "Can My Device?" rule engine | **Đã xong (2026-08-31)** — package mới `feat/canmydevice/{CanMyDeviceEvaluator,CanMyDeviceProvider,CanMyDeviceBottomSheet,CanMyDeviceBar}.kt`, entry point là 1 bar tap-to-open trong `FrmCpuInfo.kt` ngay dưới AI Readiness bar (cùng khuôn mẫu `AiReadinessBar`/`AiReadinessBottomSheet`). 6 rule MVP, mỗi rule tái dùng dữ liệu 1 `DataProvider` đã có sẵn (không đọc raw system API mới): Netflix/streaming HD (Widevine security level — L1 = yes, L2 = **partial**, L3/không hỗ trợ = no), 4K DRM streaming (HDCP ≥ 2.2 từ `maxHdcpLevel`/`hdcpLevel`), chụp RAW (DNG) (`CameraLensData.hasRawCapture`), quay slow-motion 240fps+ (`maxSlowMotionFps`), chơi game nặng qua Vulkan (version ≥ 1.1 = yes, 1.0 = **partial**, không có = no), chạy AI/LLM on-device nhỏ (tái dùng thẳng `AiReadinessProvider`/`AiReadinessEvaluator.Tier`, không tính lại). **Scope hẹp hơn đề xuất Codex**: KHÔNG có rule pack cập nhật từ xa, KHÔNG VIP gating, KHÔNG dùng thêm nguồn Media codec (AV1/HEVC) hay HDR màn hình — 6 nguồn đã đủ đa dạng (Drm×2, Camera×2, Gpu×1, AiReadiness×1) và tránh đụng vào tab `screen` đang có việc migrate T2.x riêng. Verdict enum YES/NO/PARTIAL — PARTIAL chỉ phát sinh tự nhiên ở 2 rule có tier trung gian thật sự trong dữ liệu gốc (Widevine L2, Vulkan 1.0-only, AI tier BASIC), không ép buộc ở các rule còn lại. **Verify**: `CanMyDeviceEvaluatorTest` 23 test (YES/NO/PARTIAL mỗi rule + edge case null/unparseable), `testDevDebugUnitTest` 200/200 pass, `assembleDevDebug` OK | Codex |
| ✅U06 | CPU topology visualizer | **Đã xong (2026-09-02)** — Sprint 7 (`bfa5e32`, F09/U06) làm phần group core theo cluster (Prime/Performance/Efficiency), phiên này (Sprint 8) hoàn tất phần còn lại: **cache hierarchy per-cluster** — `cpuinfo-libs.cpp` thêm 8 JNI export mới đọc `processor_start`/`processor_count` (trước chỉ đọc `.size`) cho cả 4 mức L1i/L1d/L2/L3; `ClusterTopologyBuilder.build()` nhận thêm `rawCaches: List<RawCache>` (chip-wide, chưa gắn cluster) và tự match instance nào thuộc cluster nào bằng `processorStart in coreRange` (pure logic, test được không cần mock native); `ClusterTopologyScreen` hiện thêm dòng "L1i: 64.00 KB (riêng)" / "L2: 2.00 MB (chung ×8)" mỗi cluster. **ISA extension per-cluster — chính thức KHÔNG LÀM**, xác minh thực nghiệm bằng 2 cách độc lập trên cả TECNO KJ7 (A76+A55) và Galaxy S24 Ultra (3 uarch khác nhau): (1) `struct cpuinfo_processor` trong `cpuinfo.h` không có field ISA nào — ISA chỉ có 1 struct global `cpuinfo_isa` toàn chip; (2) `/proc/cpuinfo` (nguồn dữ liệu khác, không qua libcpuinfo) — dòng `Features:` giống hệt nhau trên MỌI core dù `CPU part` (uarch) khác nhau rõ rệt giữa các core. Kết luận: dữ liệu phân biệt ISA theo core/cluster không tồn tại ở cả 2 nguồn khả dĩ trên Android, không phải giới hạn code. **Bug found & fixed trong lúc viết test**: MockK không mock được `external fun` (native method không có bytecode body để proxy đè lên) — `UnsatisfiedLinkError` ngay tại `every {}` đầu tiên; refactor matching logic từ `ClusterTopologyProvider` (native-calling glue) sang `ClusterTopologyBuilder` (pure) để unit-test được mà không cần mock, đúng convention Provider/Builder đã có sẵn trong file. **Verify**: `ClusterTopologyBuilderTest` (+3 test), `ClusterTopologyScreenTest` (widget, 3 test mới, render Compose trực tiếp không qua Activity), `ActHostSmokeTest#clusterTopologyCardsShowAtLeastOneCacheLevelOnDefaultCpuTab` (integration, dữ liệu native thật) — toàn bộ + suite cũ 24/24 pass trên cả TECNO KJ7 và Galaxy S24 Ultra thật, kèm ảnh chụp xác nhận hiển thị đúng cả 2 máy | Claude CLI |
| ✅U07 | VIP diagnostic report lịch sử | **Đã xong (2026-09-01)** — package mới `feat/vipreport/{VipDiagnosticEvaluator,VipDiagnosticReportRepository,VipDiagnosticReportBottomSheet}.kt`, entry point trong `FrmSettings.kt` (mục "VIP Diagnostic History" trong category VIP). **Data**: `VipDiagnosticSnapshot` (battery level %, designed capacity, charge counter mAh, cycle count API34+, health, RAM available %, storage free/total) — snapshot thủ công qua nút "Save today's snapshot", không tự động chạy nền qua WorkManager (đơn giản hoá có chủ đích, tránh scope creep sang lịch trình tự động). **Persist**: tái dùng đúng pattern `HardwareSnapshotProvider` (1 key DataStore, Gson JSON) nhưng generalize từ 1 baseline → `List<Snapshot>`, cap `MAX_HISTORY_ENTRIES=24`, evict cũ nhất khi vượt cap. Dedup theo ngày: lưu 2 lần cùng ngày → ghi đè thay vì thêm dòng mới. **Trend**: chỉ tính delta số chu kỳ sạc (cycle count) giữa mốc đầu/cuối — bỏ qua "capacity health %" vì charge counter chỉ có ý nghĩa khi cùng mức sạc pin, ép user sạc đầy mỗi lần lưu không hợp UX "lưu tuỳ ý". **VIP gate**: dùng `AdManager.isVipByKeyActive()` (cách check VIP DUY NHẤT có sẵn trong app — trước giờ chỉ dùng cho hiển thị ad/badge, đây là feature nội dung đầu tiên gate theo VIP thật) — không VIP thì điều hướng thẳng sang `ActVip` (giống hành vi "VIP Management"). **Verify**: `VipDiagnosticEvaluatorTest` (5 test JVM, logic tóm tắt trend) + `VipDiagnosticReportRepositoryInstrumentedTest` (5 test, real BatteryManager/DataProviderRam/DataStore nhưng file DataStore throwaway — không đụng dữ liệu thật) + `VipDiagnosticContentTest` (3 test widget, render Compose trực tiếp không qua Activity/BottomSheetDialog, cũng throwaway DataStore) + `ActHostSmokeTest#vipDiagnosticHistoryPrefRoutesSomewhereWithoutCrashing` (wiring/gate thật). Toàn bộ 4 file test = 14 test method, chạy pass ổn định 3 lần liên tiếp trên Galaxy S24 Ultra thật. **Phát hiện phụ lúc test**: `appPermissionsActionShowsPermissionsSheet` (Sprint 5 cũ) fail trên S24U dù pass trên TECNO — S24U có nhiều app cài sẵn hơn nên hàng "chính app" không kịp load/composed trong LazyColumn ở thời điểm search; sửa bằng cách thêm `testTag` cho `LazyColumn` + `performScrollToNode` retry trong `waitUntil` (tự hồi phục dù nguyên nhân là chưa compose hay chưa load xong data) | Claude CLI |
| ✅U08 | QS tile family mở rộng / "Smart Combo Tile" | **Đã xong (Sprint 18, 2026-08-30)** — 2 tile mới `feat/batterytile`/`feat/networktile`, tận dụng đúng pattern `cputile`/`ramtile` sẵn có (tap-to-open qua `feat/qstile/QsTileNavigation.kt` — helper mới dùng chung cho cả 4 tile). **Scope hẹp hơn đề xuất Gemini CLI**: KHÔNG gộp thành 1 tile chọn chỉ số ưu tiên qua config UI — cần thêm Activity cấu hình + lưu preference cho lợi ích không rõ hơn việc Android đã cho user tự thêm/sắp xếp nhiều tile độc lập trong panel gốc. Material You dynamic tint: QS tile icon đã tự động được hệ thống tint theo theme, không cần code riêng. Network tile chỉ hiện loại kết nối (Wi-Fi/Mobile/Ethernet/Offline), không hiện cường độ tín hiệu — cần permission vị trí cho RSSI Wi-Fi (xem flow permission ở `feat/infor/network`), không hợp 1 tile 0-permission. **Bug thật phát hiện lúc refactor** (không phải lúc test — phát hiện qua đối chiếu `api-versions.xml` của Android SDK khi viết helper dùng chung): `ServiceRamTile.onClick()` gọi `startActivityAndCollapse(PendingIntent)` (chỉ tồn tại từ API 34) trên cả nhánh API 24-33 → `NoSuchMethodError` crash 100% khi bấm tile RAM Cleanup trên Android 7-13 (trái ngược changelog B07 tưởng đã fix xong). Đã sửa cùng lúc khi gộp logic vào helper dùng chung. Verify trên máy thật (Galaxy S24 Ultra, API 36): thêm cả 4 tile vào QS panel, tap Battery/Network tile mở app đúng, không crash, logcat sạch | **[đồng thuận 2 nguồn]** Claude CLI + Gemini CLI |
| ✅U09 | VIP streak — daily check-in | **Đã xong** — xác nhận qua code hiện tại (`feat/vip/streak/{CheckInStreak,CheckInStreakPrefs}.kt`, gọi từ `ActHost.evaluateDailyStreak()`), doc này trước đó chưa gắn nhãn dù đã ship. Mở app N ngày liên tiếp tính streak, milestone 7-ngày nudge sang Shield Score sheet | scan ads/infra |
| ✅U10 | "Shield Score" — Device Health Score badge | **Đã xong** — xác nhận qua code hiện tại (`feat/shield/{ShieldScoreProvider,ShieldScoreCalculator,ShieldScoreBottomSheet}.kt`, badge cạnh icon VIP toolbar, test `shieldScoreBadgeOpensBottomSheetWithScoreAndStreak` trong `ActHostSmokeTest`), doc này trước đó chưa gắn nhãn dù đã ship. Gộp RAM/Storage/Battery thành 1 điểm 0-100 | scan ads/infra |
| ⏸️U11 | VIP "gift a day" share loop | VIP user generate mã 1 lần tặng bạn +1 ngày VIP (giới hạn 1 mã/ngày) → chia sẻ qua Intent.ACTION_SEND có sẵn. Tạo viral loop nhẹ cho app read-only vốn không có social feature | scan ads/infra |
| | **Deferred lần 2 (2026-09-02)** — hỏi lại user sau khi hết việc trong epic-03/quick_win, user chọn "chưa, để sau" lần nữa. Không phải vấn đề kỹ thuật — vẫn là ý tưởng hợp lệ, revisit khi được yêu cầu lại. | |
| ✅U13 | Widget "Điểm sức khoẻ máy" (Shield Score) trên màn hình chính | **Đã xong (2026-09-02)** — package mới `feat/shieldwidget/{ShieldScoreWidgetProvider,ShieldScoreWidgetUpdateWorker}.kt`, đúng khuôn `feat/ramwidget/` (AppWidgetProvider + WorkManager periodic 15 phút, không Hilt vì `DataProviderRam`/`BatteryStatusProvider` constructor thuần framework). Nguồn điểm: `ShieldScoreProvider`/`ShieldScoreCalculator` có sẵn (U10), không có logic tính điểm mới. Layout `widget_shield_score.xml` tái dùng `@drawable/widget_ram_background`. Màu số điểm theo 3 ngưỡng y hệt `ShieldScoreBottomSheet`'s `scoreColor()` (xanh ≥80/cam ≥50/đỏ <50) — đặt bằng `Int` literal thay vì gọi `android.graphics.Color.parseColor()` runtime vì hàm đó trả về 0 dưới cấu hình test `unitTests.isReturnDefaultValues = true` của project, sẽ làm gãy unit test nếu gọi thật. Tap widget mở thẳng `ShieldScoreBottomSheet` qua extra `EXTRA_OPEN_SHIELD_SCORE` trên Intent, xử lý trong `ActHost.onCreate()` (`maybeOpenShieldScoreFromWidget()`) — cơ chế mới, chưa có tiền lệ trong `RamWidgetProvider` (widget đó chỉ mở app, không mở sâu 1 sheet cụ thể). **Edge case phát hiện lúc verify thật, chấp nhận không sửa**: nếu app cài mới hoàn toàn VÀ user tap widget trước khi từng mở app 1 lần, `maybeShowFirstLaunchLanguagePicker()` (chạy async, độc lập) có thể hiện sheet chọn ngôn ngữ đè lên sheet Shield Score vừa mở — race hiếm gặp (cần cài mới + thêm widget + tap widget mà chưa từng mở app), không sửa vì effort/rủi ro của fix (đồng bộ 2 luồng async) không tương xứng với tần suất xảy ra thật. **Verify**: `ShieldScoreWidgetProviderTest` (3 test: schedule/cancel WorkManager, ngưỡng màu đúng 3 dải), build/lint xanh, verify thật trên máy OPPO CPH1989 thật (đặt widget qua UI launcher thật — không phải giả lập, `adb shell cmd appwidget` không hỗ trợ trên máy này) — widget hiện đúng điểm số + màu, tap mở đúng sheet "Điểm sức khoẻ thiết bị" khớp số liệu, đã xoá widget khỏi máy sau khi verify xong (máy cá nhân thật của user) | Claude CLI |
| ✅U14 | Ảnh "thẻ thông tin máy" để chia sẻ (Device ID Card) | **Đã xong (2026-09-02)** — package mới `feat/devicecard/{DeviceCardData,DeviceCardProvider,DeviceCardRenderer,DeviceCardExporter}.kt`. `DeviceCardProvider` gom dữ liệu từ các `DataProvider*`/`ShieldScoreProvider` có sẵn (không đọc hệ thống mới). `DeviceCardRenderer` vẽ trực tiếp bằng `Canvas`/`Paint`/`StaticLayout` (không Compose `captureToImage()`, đúng plan ban đầu) ra `Bitmap` cố định 1080×1350 — panel trắng bo góc trên nền `@color/primary`, badge tròn Shield Score góc trên phải (3 màu theo ngưỡng y hệt `ShieldScoreWidgetProvider.scoreColor()`, dùng `Int` literal không gọi `Color.parseColor()` vì lý do unit test stub y hệt U13), tên máy + chip wrap qua `StaticLayout`, 5 dòng label/value (Số nhân/RAM/Bộ nhớ/Màn hình/Android), footer. `DeviceCardExporter` lưu PNG vào `context.cacheDir`, chia sẻ qua `FileProvider` có sẵn (`file_paths.xml`'s `apk_cache`) + `ACTION_SEND` + `FLAG_ACTIVITY_NEW_TASK` (bắt buộc vì Hilt `@ApplicationContext`, không phải Activity context). **Entry point**: thêm `Format.IMAGE` vào `SystemInfoExporter.Format` enum (dùng chung, không tạo enum riêng), thêm hàng thứ 3 trong `ExportFormatBottomSheet.kt`'s picker sẵn có (TEXT/JSON/IMAGE), `ActHost.registerExportFormatResult()` branch gọi `DeviceCardExporter` khi `format == Format.IMAGE` thay vì `SystemInfoExporter`. **Verify**: `DeviceCardDataTest` (3 test unit cho `formatGb()`), `DeviceCardRendererTest` (4 test androidTest — kích thước bitmap đúng, có vẽ nội dung thật không phải canvas trắng, không crash khi score null/tên máy quá dài) pass trên cả máy thật OPPO CPH1989 và emulator, build/lint xanh, full `ActHostSmokeTest` 22/22 pass trên máy thật (1 lần đầu fail `sensorTestFabRunsThroughAllStepsToADoneScreen` do timeout Compose animation — xác nhận flaky, pass lại ngay khi rerun riêng, không liên quan gì tới thay đổi U14). Verify tay trên máy thật: bấm icon share → chọn "Hình ảnh (chia sẻ)" → share sheet Android hiện đúng ảnh PNG (Facebook hiện thêm option "Đặt làm ảnh đại diện" — dấu hiệu content là ảnh thật, không phải text) → pull file PNG từ cache, ảnh hiển thị đúng thông số máy thật (CPH1989, MediaTek MT6771V/WT, 8 nhân, RAM 7.5GB, bộ nhớ 107.7GB, 1080×2208·60Hz, Android 11, Shield Score 60 màu cam đúng dải). Dịch đủ 6 locale | Claude CLI |
| ✅U15 | Benchmark GPU (điểm hiệu năng đồ hoạ) | **Đã xong (2026-09-02)** — package mới `feat/gpubench/{GpuBenchmark,GpuBenchmarkRunner,GpuBenchResultPrefs,VMGpuBench,GpuBenchScreen,FrmGpuBench}.kt`. Khác 3 benchmark còn lại (thuần coroutine): workload chạy thật trong `GLSurfaceView.Renderer.onDrawFrame()` trên GL thread — fragment shader GLES 2.0 lặp 64 vòng sin/cos mỗi pixel (workload ALU nặng, không phụ thuộc hãng GPU cụ thể), đo FPS trung bình trong cửa sổ 5s sau 1s khởi động (warmup) để loại bỏ khung hình chậm ban đầu lúc GPU chưa lên xung nhịp. Không có bảng điểm cứng (đúng plan ban đầu) — chỉ báo FPS thô + so với lần chạy trước, vì phổ driver GPU (Mali/Adreno/PowerVR/Xclipse) quá rộng để 1 thang điểm tự chế có ý nghĩa. Tái dùng đúng ngưỡng an toàn nhiệt 43°C. `GLSurfaceView` gắn qua Compose `AndroidView` (chỉ mount khi đang chạy test, tháo ra khi xong qua `onRelease`), lifecycle `ON_PAUSE`/`ON_RESUME` được theo dõi qua `LifecycleEventObserver` — khi app bị background giữa chừng, `requestStop()` abort thẳng thành `AbortReason.INTERRUPTED` thay vì để timer tiếp tục chạy ngầm trong lúc `GLSurfaceView` bị pause (nếu không xử lý sẽ tính sai FPS vì elapsed time vẫn trôi mà không có frame nào được vẽ). **Verify**: `GpuBenchmarkTest` (3 test unit: fps(), ngưỡng nhiệt), `GpuBenchScreenTest` (6 test androidTest — pass cả máy thật OPPO CPH1989 và emulator, dùng `GLSurfaceView.Renderer` no-op thay renderer thật vì chỉ test tầng Compose không phải frame timing thật), build/lint xanh. Verify tay đầy đủ trên máy thật: shader workload vẽ đúng hoạ tiết moiré thật (chứng minh không phải placeholder), đo được 50.3-52.3 FPS qua 3 lần chạy (MediaTek MT6771, màn 60Hz — hợp lý cho workload nặng), "Lần trước" hiện đúng qua các lần chạy liên tiếp, bấm Dừng giữa chừng đúng ra "Test đã dừng trước khi xong." (`AbortReason.INTERRUPTED`) và không lưu kết quả abort vào "Lần trước" (đúng thiết kế), không crash. `ActHostSmokeTest` full suite: môi trường máy thật hôm nay không ổn định (log bị cắt giữa chừng do lỗi gRPC/USB nhiều lần, không liên quan code — đã xác nhận qua nhiều lần chạy lại), nhưng trong mọi lần chạy dù đầy đủ hay một phần, **lỗi duy nhất từng thấy** là `sensorTestFabRunsThroughAllStepsToADoneScreen` (ComposeNotIdleException animation timeout, không đụng gì tới GPU benchmark) — chạy cô lập lại không còn exception trong logcat, xác nhận flaky cũ đã biết chứ không phải regression. Chỉ có 1 máy thật (OPPO CPH1989, chip Mali) để test trong đợt này — chưa test được trên Adreno/Xclipse/PowerVR như rủi ro đã lường trước, cần lưu ý khi nhận báo cáo lỗi từ thiết bị hãng GPU khác | Claude CLI |
| ✅U16 | Benchmark RAM (tốc độ đọc/ghi bộ nhớ) | **Đã xong (2026-09-02)** — package mới `feat/rambench/{RamBenchmark,RamBenchmarkRunner,RamBenchResultPrefs,VMRamBench,RamBenchScreen,FrmRamBench}.kt`, đúng khuôn `feat/storagebench/` nhưng thao tác trên `ByteArray` 32MB (`System.arraycopy` ghi/đọc lặp lại 500ms/phase, không phải fixed-size-1-pass vì memory copy quá nhanh để đo chính xác 1 lần) thay vì file cache. **An toàn thêm so với Storage Benchmark**: kiểm `Runtime.getRuntime().maxMemory()` trước khi cấp phát — cần ≥4x buffer size còn trống mới chạy, nếu không đủ thì abort với lý do riêng (`AbortReason.INSUFFICIENT_MEMORY`, khác `OVERHEAT`) thay vì liều cấp phát rồi crash `OutOfMemoryError`. Tab mới vị trí 17 trong ViewPager Hardware (`AdtInfoContainerState.kt`). **Verify**: `RamBenchmarkTest` (5 test: mbPerSec, ngưỡng nhiệt, 2 test cho `hasEnoughMemory`), `RamBenchScreenTest` (widget, 5 test — render trực tiếp không qua Activity, **chứng minh được** dù Storage Benchmark/Dashboard trước đó bỏ qua kiểu test này vì lý do khác: harness NPE của 2 tab đó chỉ xảy ra khi `ActHostSmokeTest` điều hướng qua ViewPager bằng `composeRule` gắn Activity, không phải khi render Screen composable độc lập) — full suite 22/22 pass trên máy OPPO CPH1989 thật, chạy tay 2 lần benchmark thật trên máy (Ghi 4601.8 MB/s / Đọc 4088.3 MB/s lần 1, xác nhận "Lần trước" hiện đúng ở lần 2), không crash/OOM | Claude CLI |
| | **Thứ tự đề xuất nếu làm cả 4 (2026-09-02)**: U13 → U16 → U14 → U15. Cả 4 ý đã xong (2026-09-02). | |
| ✅U17 | Bộ test tổng hợp "Chạy tất cả" (All-in-one Benchmark Suite) | **Đã xong (2026-09-02)** — package mới `feat/allbench/{VMAllBench,AllBenchScreen,FrmAllBench}.kt`. **Không tạo `AllBenchRunner` riêng như plan ban đầu** — orchestration đủ mỏng để nằm thẳng trong `VMAllBench` (gọi tuần tự `ThrottleTestRunner`/`StorageBenchmarkRunner`/`RamBenchmarkRunner`'s `run(onState)` suspend callback có sẵn, không sửa logic đo nào), tránh thêm 1 lớp gián tiếp không cần thiết. Riêng GPU (state kiểu `StateFlow`, không phải suspend callback, vì driver bởi GL thread thật) được `await` qua `gpuRunner.state.first { Finished/Aborted }` sau khi `gpuRunner.start()` — VM set `UiState.Running(Step.GPU)` khiến `AllBenchScreen` mount `GpuSurfaceView` (composable dùng chung, tách ra từ `GpuBenchScreen.kt` để 2 màn không trùng lặp code GLSurfaceView+lifecycle pause/resume/interrupt). **An toàn giữa các bước**: 1 bước abort (quá nhiệt/thiếu bộ nhớ/user dừng) → dừng toàn bộ suite ngay, không chạy tiếp bước sau. Tab mới vị trí 19 trong ViewPager Hardware. **Verify**: `AllBenchScreenTest` (5 test androidTest — Idle/Running mỗi bước/Done/Aborted) pass 5/5 trên máy thật OPPO CPH1989, build/lint xanh. Verify tay đầy đủ trên máy thật: chạy hết cả 4 bước tuần tự thật (CPU 1749MHz, Bộ nhớ 109.2/660.4 MB/s, RAM 2108.2/2179.7 MB/s, GPU 60.1 FPS — tổng thời gian sát đúng ước tính ~40-50s), share text đúng mở system chooser, bấm Dừng ngay lúc đang ở bước Throttle ra đúng "Đã dừng ở bước Kiểm tra nhiệt & giảm xung", không crash. `ActHostSmokeTest` full suite: pass sạch 22/22 trên Galaxy S24 Ultra thật (không cả lỗi flaky sensor cũ) — máy OPPO CPH1989 hôm nay gặp lỗi môi trường USB/gRPC liên tục (không liên quan code, đã gặp lặp lại nhiều lần trong các đợt verify trước đó của U14/U15) nên dùng kết quả sạch từ S24U làm bằng chứng chính | Claude CLI |
| ✅U18 | Lịch sử benchmark theo thời gian (Benchmark History + Trend Chart) | **Đã xong (2026-09-02)** — cả 4 `*ResultPrefs` (`ThrottleResultPrefs`/`StorageBenchResultPrefs`/`RamBenchResultPrefs`/`GpuBenchResultPrefs`) đổi từ lưu 1 record key-value đơn sang `getHistory(): List<SavedResult>` dạng JSON (Gson, đúng pattern `VipDiagnosticReportRepository`), cap `MAX_HISTORY_ENTRIES = 24`, **không dedup theo ngày** (khác VIP diagnostic — chạy cùng benchmark nhiều lần trong ngày, vd trước/sau khi máy nguội, là so sánh thật có ý nghĩa, không phải nhiễu cần gộp). **Không migrate dữ liệu cũ** — quyết định đơn giản hoá theo đúng rủi ro đã lường trong plan, bắt đầu lịch sử mới tinh (đổi tên key SharedPreferences, dữ liệu "lần trước" cũ mất, chấp nhận được vì app chưa phát hành bản có tính năng này). Component mới `ui/component/BenchTrendChart.kt` — 1 composable `LineChart` (MPAndroidChart, tái dùng đúng lib project đã có, **không thêm dependency mới**) dùng chung cho cả 4 màn "Done" qua Compose `AndroidView`, ẩn hẳn nếu <2 điểm dữ liệu. Trục X là số thứ tự lần chạy (không phải elapsed-time như `ElapsedSecondsAxisFormatter` của Dashboard — benchmark chạy thưa/không đều, số lần chạy đơn giản và đủ ý nghĩa hơn). Mỗi benchmark chart 1 chỉ số đại diện (Throttle: `sustainedFreqMhz`; Storage: `seqWriteMbPerSec`; RAM: `writeMbPerSec`; GPU: `avgFps`) — không cố gộp nhiều chỉ số/nhiều đơn vị đo vào 1 chart. Sửa `axisLeft.setLabelCount(3, false)` sau khi phát hiện thật lúc verify: label trục Y mặc định bị rối khi giá trị 2 lần chạy gần nhau (vd 120.1 vs 120.2 FPS ra 6 nhãn gần trùng nhau). **Verify**: widget test mở rộng ở cả 4 file test hiện có (`GpuBenchScreenTest`/`RamBenchScreenTest`/`ThrottleScreenTest` cập nhật `history` param, thêm 1 test chart-render-without-crashing cho GPU) pass sạch cả 2 máy thật (OPPO CPH1989 + Galaxy S24 Ultra); build/lint xanh. Verify tay đầy đủ trên Galaxy S24 Ultra: chạy GPU benchmark 3 lần (120.1→120.2→120.2 FPS) và RAM benchmark 2 lần (ghi 34188.9→35976.7 MB/s), chart hiện đúng xu hướng tăng cả 2 lần, trục Y gọn sau khi sửa, "Lần trước" vẫn đúng song song với chart, dữ liệu persist đúng qua cả app restart. `ActHostSmokeTest` 22/22 pass sạch trên S24U (OPPO CPH1989 tiếp tục gặp lỗi môi trường USB/gRPC không liên quan code, như các đợt verify U15/U17 trước) | Claude CLI |
| ✅U19 | Cảnh báo sức khoẻ máy chủ động (Proactive Health Alert Notification) | **Đã xong (2026-09-02)** — package mới `feat/healthalert/{HealthAlertLogic,HealthAlertPrefs,HealthAlertScheduler,HealthAlertWorker}.kt` + `appinitializers/HealthAlertNotificationInitializer.kt`. `HealthAlertLogic` (pure) quyết định có cảnh báo hay không: giảm ≥15 điểm so lần check trước, HOẶC điểm hiện tại dưới ngưỡng tuyệt đối 40, VÀ tối đa 1 lần/24h (cooldown chống spam). `HealthAlertWorker` (plain `CoroutineWorker`, không Hilt, đúng lý do `ShieldScoreWidgetUpdateWorker` đã dùng) đọc `ShieldScoreProvider.compute()` có sẵn (không tính điểm mới), so sánh, tạo `Notification` dẫn vào `ShieldScoreBottomSheet` qua đúng cơ chế `EXTRA_OPEN_SHIELD_SCORE` U13 đã có. **Permission**: thêm `POST_NOTIFICATIONS` vào Manifest, xin quyền **chỉ khi user tự bật toggle** trong Settings ("Cảnh báo sức khoẻ máy", `SwitchPreferenceCompat` mới trong `pref.xml`/`FrmSettings.kt`) — không xin ở first launch. Từ chối quyền → toggle tự tắt lại, im lặng, không nhắc lại. **Scheduling**: `WorkManager` periodic 6 giờ (khác 15 phút của widget — đây là check nền, không phải widget refresh liên tục), bật/tắt qua `HealthAlertScheduler.schedule()`/`cancel()`. **Refactor sau khi phát hiện lỗ hổng verify lúc audit**: tách phần quyết định-và-bắn-thông-báo ra khỏi `HealthAlertWorker` thành `HealthAlertNotifier.maybeNotify(context, prefs, score, now)` — object thuần nhận `Context` thật, không cần khởi tạo `CoroutineWorker`/`WorkerParameters` (class không có constructor public ngoài dependency test-only `androidx.work:work-testing`). Nhờ vậy verify được **thật sự có notification bắn ra hay không**, không chỉ dừng ở logic quyết định + lịch WorkManager. **Verify**: unit test `HealthAlertLogicTest` (6 test) + `HealthAlertSchedulerTest` (2 test MockK), build/lint xanh. `HealthAlertNotifierTest` (androidTest mới, 3 test — cấp quyền qua `UiAutomation.grantRuntimePermission` thay vì thêm dependency `androidx.test:rules`) xác nhận **notification thật được post/không post đúng** qua `NotificationManager.getActiveNotifications()`: điểm dưới ngưỡng → có notification; điểm cao không giảm → không notification; gọi lần 2 trong cooldown → không notification lặp — pass sạch 3/3 trên máy thật Pixel 7 Pro. Verify tay đầy đủ trên Galaxy S24 Ultra thật (trước khi refactor): bật toggle → dialog xin quyền thông báo **thật** của hệ thống hiện ra → bấm "Cho phép" → xác nhận `POST_NOTIFICATIONS granted=true` qua `dumpsys package`, toggle tự chuyển ON, `WorkManager` job thật được enqueue (xác nhận qua `dumpsys jobscheduler`, lịch chạy sau ~5h35m khớp chu kỳ 6h) → tắt toggle chuyển UI đúng ngay không cần xin quyền lại, không crash. `ActHostSmokeTest` trên Pixel 7 Pro (máy kết nối sau, chạy Android 17/API 37 preview): 22/22 fail toàn bộ do `Espresso InputManagerEventInjectionStrategy` không tương thích OS quá mới — cùng loại lỗi môi trường trước đây chỉ gặp trên emulator, không liên quan gì thay đổi U19 (không kịch bản nào trong suite đụng health alert; `HealthAlertNotifierTest` không dùng Espresso touch injection nên không bị ảnh hưởng, pass sạch trên chính máy này) | Claude CLI |
| | **Thứ tự đề xuất đợt 2 (2026-09-02)**: U17 → U18 → U19. Cả 3 ý đã xong (2026-09-02). | |
| ✅U20 | Biểu đồ lịch sử pin trong VIP Diagnostic History | **Đã xong (2026-09-02)** — tái dùng thẳng `ui/component/BenchTrendChart.kt` (U18) trong `VipDiagnosticReportBottomSheet.kt`'s `HistoryState` composable, phía trên danh sách `SnapshotRowCard`. Thêm `VipDiagnosticEvaluator.batteryLevelSeries(history): List<Double>` (pure, lọc sentinel `-1`) — điểm khác U18: field pin có thể là sentinel không đọc được, `BenchTrendChart` không tự lọc nên cần hàm riêng trước khi đưa vào chart. Ẩn hẳn nếu <2 điểm hợp lệ. Không tab/màn mới, không permission mới, vẫn VIP-gated như cũ. **Verify**: unit test `VipDiagnosticEvaluatorTest` (+3 test: rỗng, ánh xạ đúng thứ tự, lọc sentinel), widget test `VipDiagnosticContentTest` (+2 test — seed trực tiếp 2 snapshot khác ngày lịch để né dedup cùng-ngày, xác nhận chart title hiện/ẩn đúng theo số điểm) — cả 2 pass sạch 8/8 (bao gồm 3 test cũ) trên **OPPO CPH1989 thật**. **Bug thật phát hiện lúc chạy trên máy**: 2 test mới ban đầu viết `fun test() = runBlocking { ... assertExists() }` — JUnit yêu cầu test method trả về `void`, nhưng `= runBlocking { }` khiến hàm suy ra kiểu trả về là kết quả biểu thức cuối (`SemanticsNodeInteraction`, không phải `Unit`) → `InvalidTestClassError` khi chạy thật (không phát hiện được lúc compile, chỉ lộ ra khi instrumentation runner validate class trên thiết bị) — sửa bằng block body tách `runBlocking { }` riêng khỏi phần còn lại của hàm. **Pixel 7 Pro (Android 17 preview)**: mọi test dùng `createComposeRule()` (bao gồm `VipDiagnosticContentTest`) fail toàn bộ do lỗi môi trường Espresso/`InputManagerEventInjectionStrategy` đã ghi nhận ở U19 — không liên quan code. **Verify tay trên Pixel 7 Pro bị gián đoạn**: màn VIP Diagnostic History có VIP-gate thật (`AdManager.isVipByKeyActive()`), máy chưa có VIP — thử mở khoá qua "Xem quảng cáo → 3 ngày VIP" (rewarded ad thật, dev flavor dùng AppLovin MAX test ad), nhưng dừng giữa chừng theo quyết định của user (không muốn tiếp tục xem ad) — **chưa verify tay chart pin thật trên Pixel**, dùng kết quả widget test thật trên CPH1989 (repository thật qua DataStore tạm, không phải mock) làm bằng chứng chính thay thế | Claude CLI |
| ✅U21 | Widget "Kết quả benchmark gần nhất" | **Đã xong (2026-09-03)** — package mới `feat/lastbenchwidget/{LastBenchPicker,LastBenchWidgetProvider,LastBenchWidgetUpdateWorker}.kt`, đúng khuôn `ShieldScoreWidgetProvider` (U13). **Phát hiện lại lúc code, khác giả định lúc lên plan**: cả 4 `*ResultPrefs` tuy `@Inject`-annotated cho Hilt nhưng constructor vẫn là `public (Context)` bình thường — `@ApplicationContext`/`@Inject` chỉ là marker cho Hilt, **không** chặn gọi trực tiếp `ThrottleResultPrefs(context.applicationContext)` giống hệt cách `ShieldScoreWidgetProvider` đã tự khởi tạo `DataProviderRam`/`BatteryStatusProvider` — plan cũ lo "Hilt-only" là **hiểu nhầm**, thực tế không cần đọc thẳng `SharedPreferences`/tự decode Gson như dự tính, dùng lại nguyên `getLastResult()` có sẵn của cả 4 lớp. `LastBenchPicker.pick()` (pure, unit-testable) chọn kết quả có `timestampMs` lớn nhất trong 4 nguồn — 1 widget duy nhất, không hỏi user chọn loại nào. **Tap-to-open-tab — việc mới thật sự đúng như lo ngại lúc plan**: expose 5 hằng số vị trí tab (`THROTTLE_POS`=11/`STORAGE_BENCH_POS`=16/`RAM_BENCH_POS`=17/`GPU_BENCH_POS`=18) từ `private` sang `internal` trong `AdtInfoContainerState`; `FrmInfoContainer` thêm companion `var pendingTabPosition: Int?` (one-shot) đọc trong `onViewCreated` sau khi `ViewPager2` adapter attach, set `binding.vp.setCurrentItem(pos, false)` rồi null lại ngay. `ActHost` đọc extra `EXTRA_OPEN_BENCH_TAB` và set `pendingTabPosition` **trước** `setContentView()` — bắt buộc, vì `app:navGraph` trên layout khiến `NavHostFragment` tạo + attach `FrmInfoContainer` (start destination) đồng bộ ngay trong lúc inflate, trước khi bất kỳ code nào sau `setContentView()` trong `onCreate` kịp chạy. **Giới hạn đã biết, kế thừa từ `EXTRA_OPEN_SHIELD_SCORE` (U13), không phải regression mới**: nếu app đang mở sẵn ở foreground, `am start`/PendingIntent chỉ tới `onNewIntent` (không tồn tại, không override) chứ không phải `onCreate` lại — tap-to-open-tab chỉ hoạt động khi mở app từ trạng thái nguội (đúng use case chính của widget: tap từ home screen). **Verify**: unit test `LastBenchPickerTest` (6 case: null hết/1 kết quả/chọn đúng timestamp lớn nhất/timestamp bằng nhau) + `LastBenchWidgetProviderTest` (schedule/cancel WorkManager MockK, `tabPositionFor` map đúng 4 kind) — pass sạch, không regression toàn bộ unit suite. `LastBenchWidgetProviderInstrumentedTest` (4 test — inflate `RemoteViews` thật, rỗng/CPU MHz/GPU FPS/end-to-end với `*ResultPrefs` thật) pass 4/4 trên cả Pixel 7 Pro và TECNO BG6 (không dùng Espresso/`createComposeRule` nên không dính lỗi môi trường Pixel). **Smoke test tay trên TECNO BG6** (đổi máy giữa chừng theo yêu cầu mới của user, không dùng Pixel nữa): mô phỏng tap widget bằng `am start --ei EXTRA_OPEN_BENCH_TAB <pos>` sau `am force-stop` (cold start, đúng kịch bản tap từ home screen) cho cả 5 giá trị — 11→"Stress Test" (Throttle), 16→"Storage Benchmark", 17→"RAM Benchmark", 18→"GPU Benchmark", 19→"Run All" (AllBench) — **cả 5 mở đúng tab**, xác nhận bằng screenshot thật. `ActHostSmokeTest` 22/22 + full androidTest suite 84/84 (trừ 1 flake `HealthAlertNotifierTest` không liên quan, pass lại khi chạy riêng lẻ) trên TECNO BG6. Chưa đặt widget thật lên launcher home screen (chỉ mô phỏng qua Intent extra trực tiếp) — hành vi PendingIntent/RemoteViews đã verify qua instrumented test, phần UI đặt-widget-thật là thao tác launcher thuần Android không có logic riêng của app | Claude CLI |
| ✅U22 | So sánh 2 máy trực tiếp (P2P Device Compare) | **Đã xong (2026-09-03)** — package mới `feat/p2pcompare/{DeviceComparePayload,DeviceCompareEvaluator,P2PComparePayloadProvider,P2PCompareExporter,VMP2PCompare,P2PCompareScreen}.kt`, tích hợp thẳng vào `FleetCompareBottomSheet.kt` hiện có (không tạo entry point Settings riêng, đúng plan gốc) — `FleetCompareContent` nhận thêm 1 slot `@Composable () -> Unit` render `P2PCompareScreen` ngay dưới phần catalog-compare cũ. **Đúng như plan**: dùng text JSON (Gson) thay QR, không thêm dependency mới; `DeviceComparePayload` có field `version` cho tương lai (chưa enforce migration, chỉ 1 version tồn tại); `decode()` bọc validation (blank/≤0) **trong cùng try/catch** với `gson.fromJson` — phòng đúng rủi ro đã lường: Gson deserialize qua reflection có thể bypass null-check của Kotlin, để `null` lọt vào field non-null, gây NPE ở bước validate nếu không cùng 1 try/catch. `DeviceCompareEvaluator` so trực tiếp actual-vs-actual (không dung sai, khác `FleetCompareEvaluator`). **Test**: unit — `DeviceComparePayloadTest` (9 case: round-trip/whitespace/rỗng/rác/thiếu field/field ≤0), `DeviceCompareEvaluatorTest` (5 case: local/remote thắng từng field độc lập/hoà/passthrough), `VMP2PCompareTest` (7 case: state machine Export↔Result, +2 case bổ sung ở đợt audit — sửa lỗi khi edit code sau parse-error phải tự xoá cờ lỗi, và `onCompareClicked` phải no-op khi đã ở Result state); widget — `P2PCompareScreenTest` (8 test Compose thuần không Hilt, +1 test bổ sung dùng `performTextInput` gõ thật vào ô paste thay vì chỉ assert theo state truyền sẵn); integration — `P2PComparePayloadProviderInstrumentedTest` (2 test, real `DataProviderRam` + round-trip encode/decode/compare thật trên máy). **Tất cả pass sạch trên TECNO BG6**: package p2pcompare 10/10, full androidTest suite 94/94 (0 regression, kể cả `HealthAlertNotifierTest` — flake trước đó không tái hiện), full unit suite JVM xanh, lint exit 0. **Smoke test tay đầy đủ trên TECNO** (mở Settings → Fleet Compare → thấy đúng section P2P mới): (1) bấm "Share this device's code" → mở đúng share sheet Android thật với JSON thật `{"deviceModel":"TECNO KJ7","ramBytes":8058396672,...}`; (2) dán mã JSON hợp lệ giả lập máy khác (RAM 16GB/Storage 256GB) → bấm Compare → hiện đúng kết quả 2 cột "TECNO KJ7" (7GB/228GB) vs "TestPhone" (14GB/238GB), số liệu khớp tính toán thật; (3) bấm Back → về đúng Export state rỗng, không giữ mã cũ; (4) dán "garbage" → bấm Compare → field viền đỏ + text "Invalid code, please check and try again" đúng như thiết kế. Không phát hiện bug nào trong lúc smoke test | Claude CLI |
| ✅U23 | Ảnh thẻ kết quả benchmark tổng hợp (Bench Result Card, mở rộng Device Card) | **Đã xong (2026-09-02)** — package mới `feat/benchresultcard/{BenchResultCardRenderer,BenchResultCardExporter}.kt`, copy-paste-adapt đúng như plan từ `DeviceCardRenderer`/`DeviceCardExporter` (U14): cùng `CARD_WIDTH_PX`/`CARD_HEIGHT_PX`/margin/badge-tròn/`drawWrappedText`/`FileProvider`+cache dir, đổi 4 dòng nội dung thành CPU/Bộ nhớ/RAM/GPU lấy từ `VMAllBench.Results`. Thêm nút "Chia sẻ ảnh" cạnh nút "Chia sẻ" (text) trong `AllBenchScreen.kt`'s `DoneContent`, `FrmAllBench.kt` wire `onShareImageClicked` qua `BenchResultCardExporter.exportBenchResultCard(lifecycleScope, results)`. **Bug thật phát hiện lúc smoke test trên Pixel**: 3 nút (Chia sẻ/Chia sẻ ảnh/Xong) nhét chung 1 `Row` vỡ layout thật trên máy — nút "Xong" bị đẩy chữ xuống 2 dòng. Fix: tách thành `Row` chứa 2 `OutlinedButton` (Chia sẻ, Chia sẻ ảnh) + `Spacer` + `Button` "Xong" đứng riêng dòng dưới — đã verify lại bằng screenshot thật, layout đúng, không còn vỡ chữ. **Verify**: unit không áp dụng (không tách hàm format riêng); `BenchResultCardRendererTest` (3 test — kích thước bitmap đúng, có vẽ nội dung thật ≥2 màu, không crash khi kết quả toàn giá trị 0) pass 3/3 trên cả TECNO BG6 và Pixel 7 Pro (test này không dùng `createComposeRule()` nên không bị lỗi môi trường Espresso của Pixel); `AllBenchScreenTest` (+1 test `doneState_showsShareAsImageButton`) pass 6/6 trên TECNO BG6, 0/6 trên Pixel do lỗi môi trường Espresso/`InputManagerEventInjectionStrategy` đã ghi nhận từ U19/U20 (không liên quan code). **Smoke test tay trên Pixel 7 Pro (theo đúng yêu cầu "chỉ test ở Pixel")**: chạy `AllBench` thật xong (CPU 1687 MHz, Storage 143.9/1439.8 MB/s, RAM 12433.2/14425.6 MB/s, GPU 120.0 FPS), bấm "Chia sẻ ảnh" → `ChooserActivity`/`IntentResolver` mở đúng (xác nhận qua `dumpsys window`), pull PNG thật từ cache (`bench_result_card_95533098565119.png`) — nội dung đúng 100% dữ liệu thật, không lỗi render. Sau khi sửa layout, chạy lại `AllBench` lần 2 (CPU 2039 MHz, Storage 143.7/1690.9 MB/s, RAM 14114.5/18998.0 MB/s, GPU 120.0 FPS) để xác nhận layout 2 hàng nút đúng — thành công | Claude CLI |
| | **Thứ tự đề xuất đợt 3 (2026-09-02)**: U20 → U23 → U21 → U22. **Cả 4 ý đã xong (2026-09-03)**, khép lại toàn bộ batch đợt 3. | |

---

## ✅ Quyết định đã chốt (2026-08-28)

**Làm cả 3 hướng, kết hợp**: U01 + U02 + (U09+U10). Thứ tự đề xuất theo effort tăng dần, tận dụng lại infra của bước trước cho bước sau:

1. **U09 + U10 trước** (VIP streak + Shield Score) — effort thấp nhất, tái dùng VIP infra + data provider có sẵn 100%, ship được trong 1 sprint nhỏ ngay sau khi Epic 1+2 ổn định. Tạo baseline retention/monetization trước khi đầu tư feature nặng.
2. **U02 tiếp theo** (Throttling Fingerprint) — effort trung bình, tái dùng `ObservableCpuData` polling infra đã có (sau khi tối ưu ở T2.18). Có thể xuất "Hardware Health Card" chia sẻ được — vòng lặp viral nhẹ giống U11.
3. **U01 sau cùng** (Device Truth Score / Silicon Detective) — effort lớn nhất, cần nghiên cứu database chipset kỹ, nhưng giá trị kinh doanh dài hạn cao nhất và có thể tái dùng phần data thu thập được từ U02 (đọc thanh ghi/topology).
4. **U12 (AI Readiness Score) cân nhắc chèn song song U01** vì dùng chung nền `libcpuinfo` flags chưa khai thác — nếu build F10 (Epic 3) trước, U12 gần như free add-on.

**Không phải đợi hết Epic 1+2 mới bắt đầu U09/U10** — 2 ý tưởng này không đụng vùng code đang tech-debt (Applications/Storage), có thể xen kẽ ngay khi rảnh 1-2 ngày giữa các sprint bugfix.
