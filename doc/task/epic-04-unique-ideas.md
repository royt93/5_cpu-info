# Epic 4 — Tính năng độc quyền (USP / signature differentiator)

> Mục tiêu: tính năng chưa app nào trong phân khúc "system info" làm, tận dụng lợi thế kỹ thuật riêng (native `cpuinfo` lib đang under-used, hệ VIP có sẵn, `SystemInfoExporter` có sẵn khung).

## 🏆 Top pick (3 nguồn AI độc lập cùng tự đề xuất — tín hiệu rất mạnh)

### U01 — "Device Truth Score" / Chip authenticity check / "Silicon Detective"
Đối chiếu thông tin SoC thật từ native `cpuinfo` (microarchitecture, ISA, core topology, và sâu hơn: thanh ghi phần cứng `MIDR`/`MPIDR`/`REVIDR`/CPU Part number — đọc trực tiếp tầng Native, bỏ qua hoàn toàn chuỗi giả mạo trong `Build.MODEL`/`/proc/cpuinfo`) với `Build` fields, ABI, GPU/Vulkan, camera metadata → phát hiện firmware giả spec, chip dựng/re-marked, máy refurb/xách tay khai sai model, xác định silicon stepping/revision. Xuất bằng chứng từng điểm mismatch cụ thể, không chỉ 1 điểm số mơ hồ.
**Giá trị**: cực cao cho thị trường mua bán máy cũ — không competitor nào (CPU-Z, AIDA64) làm sâu tới mức "phát hiện giả mạo bằng thanh ghi phần cứng", họ chỉ hiển thị info bề mặt.
**Nguồn**: Codex (P0, "Device Truth Score") + Claude CLI ("Chip authenticity check") + Gemini CLI ("Silicon Detective — SoC Binning Verification") — **3 nguồn độc lập, không thấy bài nhau, cùng hội tụ về đúng 1 ý tưởng cốt lõi**.
**Effort**: L (cần nghiên cứu kỹ database chipset thật vs khai báo + đọc thanh ghi native).

### U02 — Throttling Fingerprint / Freq-vs-temp curve / "Hardware Degradation Audit"
Chạy workload ngắn có kiểm soát (Gemini CLI đề xuất cụ thể: stress test 60s đa luồng native), ghi xung từng cluster + thermal status + pin theo thời gian thực → "đường cong chịu tải" so sánh được trước/sau khi thay pin, update ROM, thay keo tản nhiệt. So sánh tỷ lệ suy giảm hiệu năng Cold state vs Throttled state, xuất "Hardware Health Card" đồ hoạ có thể chia sẻ (watermark VIP). Tận dụng infra `ObservableCpuData` polling đã có sẵn (Epic 2 Story 6, T2.18) + `feat/temp`.
**Giá trị**: chưa app nào trong phân khúc trực quan hoá tương quan freq-temp tốt, và "chứng nhận sức khỏe phần cứng" chia sẻ được là hook viral tự nhiên.
**Nguồn**: Codex ("Throttling Fingerprint") + Claude CLI ("Freq-vs-temp correlation live graph") + Gemini CLI ("VIP Hardware Degradation & Thermal Throttling Audit") — **3 nguồn độc lập cùng ý tưởng cốt lõi**.
**Effort**: M-L. Lịch sử dài hạn có thể là tính năng VIP-only (monetization hook tự nhiên).

### U12 — "AI Readiness Score" (mới, Gemini CLI)
Tổng hợp các cờ tập lệnh AI mà `libcpuinfo` đã có sẵn nhưng chưa khai thác (`cpuinfo_has_arm_i8mm`, `cpuinfo_has_arm_bf16`, `cpuinfo_has_arm_neon_dot`, `cpuinfo_has_arm_sve/sve2`) cùng RAM khả dụng + số core hiệu năng cao → chấm điểm "máy chạy được on-device LLM cỡ nào" (vd "Đạt chuẩn chạy LLM 3B" / "Đạt chuẩn xử lý ảnh GenAI"). Dùng chung nền tảng dữ liệu với F10 (Epic 3, NPU/AI Capability Detection).
**Giá trị**: đúng xu hướng on-device AI 2025-2026 (Gemini Nano và tương tự), chưa app system-info nào trên Play Store có chỉ số này — timing tốt.
**Nguồn**: Gemini CLI (1 nguồn, nhưng dữ liệu nền `libcpuinfo` cực kỳ cụ thể và đã sẵn có, effort MVP thấp vì F10 đã build data layer).
**Effort**: S-M nếu làm sau F10 (tái dùng data layer).

## Ý tưởng khác

| # | Ý tưởng | Mô tả ngắn | Nguồn |
|---|---|---|---|
| ✅U03 | Hardware Diff / Before-After Snapshot | **Đã xong (Sprint 9, commit `e2e96f2`)** — `feat/snapshot/{HardwareSnapshotProvider,HardwareSnapshotEvaluator,HardwareSnapshotBottomSheet}.kt`, entry point trong Settings. Doc này chưa được cập nhật lúc đó — xác nhận lại qua code + git log (2026-09-01), không phải việc mới | Codex |
| ✅U04 | Privacy-preserving Fleet Compare | **Đã xong (Sprint 14, commit `10bb213`)** — `feat/fleet/{FleetCompareProvider,FleetCompareEvaluator,FleetCompareBottomSheet,FleetSpecCatalog}.kt`. Doc này chưa được cập nhật lúc đó — xác nhận lại qua code + git log (2026-09-01), không phải việc mới | Codex |
| ✅U05 | "Can My Device?" rule engine | **Đã xong (2026-08-31)** — package mới `feat/canmydevice/{CanMyDeviceEvaluator,CanMyDeviceProvider,CanMyDeviceBottomSheet,CanMyDeviceBar}.kt`, entry point là 1 bar tap-to-open trong `FrmCpuInfo.kt` ngay dưới AI Readiness bar (cùng khuôn mẫu `AiReadinessBar`/`AiReadinessBottomSheet`). 6 rule MVP, mỗi rule tái dùng dữ liệu 1 `DataProvider` đã có sẵn (không đọc raw system API mới): Netflix/streaming HD (Widevine security level — L1 = yes, L2 = **partial**, L3/không hỗ trợ = no), 4K DRM streaming (HDCP ≥ 2.2 từ `maxHdcpLevel`/`hdcpLevel`), chụp RAW (DNG) (`CameraLensData.hasRawCapture`), quay slow-motion 240fps+ (`maxSlowMotionFps`), chơi game nặng qua Vulkan (version ≥ 1.1 = yes, 1.0 = **partial**, không có = no), chạy AI/LLM on-device nhỏ (tái dùng thẳng `AiReadinessProvider`/`AiReadinessEvaluator.Tier`, không tính lại). **Scope hẹp hơn đề xuất Codex**: KHÔNG có rule pack cập nhật từ xa, KHÔNG VIP gating, KHÔNG dùng thêm nguồn Media codec (AV1/HEVC) hay HDR màn hình — 6 nguồn đã đủ đa dạng (Drm×2, Camera×2, Gpu×1, AiReadiness×1) và tránh đụng vào tab `screen` đang có việc migrate T2.x riêng. Verdict enum YES/NO/PARTIAL — PARTIAL chỉ phát sinh tự nhiên ở 2 rule có tier trung gian thật sự trong dữ liệu gốc (Widevine L2, Vulkan 1.0-only, AI tier BASIC), không ép buộc ở các rule còn lại. **Verify**: `CanMyDeviceEvaluatorTest` 23 test (YES/NO/PARTIAL mỗi rule + edge case null/unparseable), `testDevDebugUnitTest` 200/200 pass, `assembleDevDebug` OK | Codex |
| ✅U06 | CPU topology visualizer | **Đã xong (2026-09-02)** — Sprint 7 (`bfa5e32`, F09/U06) làm phần group core theo cluster (Prime/Performance/Efficiency), phiên này (Sprint 8) hoàn tất phần còn lại: **cache hierarchy per-cluster** — `cpuinfo-libs.cpp` thêm 8 JNI export mới đọc `processor_start`/`processor_count` (trước chỉ đọc `.size`) cho cả 4 mức L1i/L1d/L2/L3; `ClusterTopologyBuilder.build()` nhận thêm `rawCaches: List<RawCache>` (chip-wide, chưa gắn cluster) và tự match instance nào thuộc cluster nào bằng `processorStart in coreRange` (pure logic, test được không cần mock native); `ClusterTopologyScreen` hiện thêm dòng "L1i: 64.00 KB (riêng)" / "L2: 2.00 MB (chung ×8)" mỗi cluster. **ISA extension per-cluster — chính thức KHÔNG LÀM**, xác minh thực nghiệm bằng 2 cách độc lập trên cả TECNO KJ7 (A76+A55) và Galaxy S24 Ultra (3 uarch khác nhau): (1) `struct cpuinfo_processor` trong `cpuinfo.h` không có field ISA nào — ISA chỉ có 1 struct global `cpuinfo_isa` toàn chip; (2) `/proc/cpuinfo` (nguồn dữ liệu khác, không qua libcpuinfo) — dòng `Features:` giống hệt nhau trên MỌI core dù `CPU part` (uarch) khác nhau rõ rệt giữa các core. Kết luận: dữ liệu phân biệt ISA theo core/cluster không tồn tại ở cả 2 nguồn khả dĩ trên Android, không phải giới hạn code. **Bug found & fixed trong lúc viết test**: MockK không mock được `external fun` (native method không có bytecode body để proxy đè lên) — `UnsatisfiedLinkError` ngay tại `every {}` đầu tiên; refactor matching logic từ `ClusterTopologyProvider` (native-calling glue) sang `ClusterTopologyBuilder` (pure) để unit-test được mà không cần mock, đúng convention Provider/Builder đã có sẵn trong file. **Verify**: `ClusterTopologyBuilderTest` (+3 test), `ClusterTopologyScreenTest` (widget, 3 test mới, render Compose trực tiếp không qua Activity), `ActHostSmokeTest#clusterTopologyCardsShowAtLeastOneCacheLevelOnDefaultCpuTab` (integration, dữ liệu native thật) — toàn bộ + suite cũ 24/24 pass trên cả TECNO KJ7 và Galaxy S24 Ultra thật, kèm ảnh chụp xác nhận hiển thị đúng cả 2 máy | Claude CLI |
| ✅U07 | VIP diagnostic report lịch sử | **Đã xong (2026-09-01)** — package mới `feat/vipreport/{VipDiagnosticEvaluator,VipDiagnosticReportRepository,VipDiagnosticReportBottomSheet}.kt`, entry point trong `FrmSettings.kt` (mục "VIP Diagnostic History" trong category VIP). **Data**: `VipDiagnosticSnapshot` (battery level %, designed capacity, charge counter mAh, cycle count API34+, health, RAM available %, storage free/total) — snapshot thủ công qua nút "Save today's snapshot", không tự động chạy nền qua WorkManager (đơn giản hoá có chủ đích, tránh scope creep sang lịch trình tự động). **Persist**: tái dùng đúng pattern `HardwareSnapshotProvider` (1 key DataStore, Gson JSON) nhưng generalize từ 1 baseline → `List<Snapshot>`, cap `MAX_HISTORY_ENTRIES=24`, evict cũ nhất khi vượt cap. Dedup theo ngày: lưu 2 lần cùng ngày → ghi đè thay vì thêm dòng mới. **Trend**: chỉ tính delta số chu kỳ sạc (cycle count) giữa mốc đầu/cuối — bỏ qua "capacity health %" vì charge counter chỉ có ý nghĩa khi cùng mức sạc pin, ép user sạc đầy mỗi lần lưu không hợp UX "lưu tuỳ ý". **VIP gate**: dùng `AdManager.isVipByKeyActive()` (cách check VIP DUY NHẤT có sẵn trong app — trước giờ chỉ dùng cho hiển thị ad/badge, đây là feature nội dung đầu tiên gate theo VIP thật) — không VIP thì điều hướng thẳng sang `ActVip` (giống hành vi "VIP Management"). **Verify**: `VipDiagnosticEvaluatorTest` (5 test JVM, logic tóm tắt trend) + `VipDiagnosticReportRepositoryInstrumentedTest` (5 test, real BatteryManager/DataProviderRam/DataStore nhưng file DataStore throwaway — không đụng dữ liệu thật) + `VipDiagnosticContentTest` (3 test widget, render Compose trực tiếp không qua Activity/BottomSheetDialog, cũng throwaway DataStore) + `ActHostSmokeTest#vipDiagnosticHistoryPrefRoutesSomewhereWithoutCrashing` (wiring/gate thật). Toàn bộ 4 file test = 14 test method, chạy pass ổn định 3 lần liên tiếp trên Galaxy S24 Ultra thật. **Phát hiện phụ lúc test**: `appPermissionsActionShowsPermissionsSheet` (Sprint 5 cũ) fail trên S24U dù pass trên TECNO — S24U có nhiều app cài sẵn hơn nên hàng "chính app" không kịp load/composed trong LazyColumn ở thời điểm search; sửa bằng cách thêm `testTag` cho `LazyColumn` + `performScrollToNode` retry trong `waitUntil` (tự hồi phục dù nguyên nhân là chưa compose hay chưa load xong data) | Claude CLI |
| ✅U08 | QS tile family mở rộng / "Smart Combo Tile" | **Đã xong (Sprint 18, 2026-08-30)** — 2 tile mới `feat/batterytile`/`feat/networktile`, tận dụng đúng pattern `cputile`/`ramtile` sẵn có (tap-to-open qua `feat/qstile/QsTileNavigation.kt` — helper mới dùng chung cho cả 4 tile). **Scope hẹp hơn đề xuất Gemini CLI**: KHÔNG gộp thành 1 tile chọn chỉ số ưu tiên qua config UI — cần thêm Activity cấu hình + lưu preference cho lợi ích không rõ hơn việc Android đã cho user tự thêm/sắp xếp nhiều tile độc lập trong panel gốc. Material You dynamic tint: QS tile icon đã tự động được hệ thống tint theo theme, không cần code riêng. Network tile chỉ hiện loại kết nối (Wi-Fi/Mobile/Ethernet/Offline), không hiện cường độ tín hiệu — cần permission vị trí cho RSSI Wi-Fi (xem flow permission ở `feat/infor/network`), không hợp 1 tile 0-permission. **Bug thật phát hiện lúc refactor** (không phải lúc test — phát hiện qua đối chiếu `api-versions.xml` của Android SDK khi viết helper dùng chung): `ServiceRamTile.onClick()` gọi `startActivityAndCollapse(PendingIntent)` (chỉ tồn tại từ API 34) trên cả nhánh API 24-33 → `NoSuchMethodError` crash 100% khi bấm tile RAM Cleanup trên Android 7-13 (trái ngược changelog B07 tưởng đã fix xong). Đã sửa cùng lúc khi gộp logic vào helper dùng chung. Verify trên máy thật (Galaxy S24 Ultra, API 36): thêm cả 4 tile vào QS panel, tap Battery/Network tile mở app đúng, không crash, logcat sạch | **[đồng thuận 2 nguồn]** Claude CLI + Gemini CLI |
| ✅U09 | VIP streak — daily check-in | **Đã xong** — xác nhận qua code hiện tại (`feat/vip/streak/{CheckInStreak,CheckInStreakPrefs}.kt`, gọi từ `ActHost.evaluateDailyStreak()`), doc này trước đó chưa gắn nhãn dù đã ship. Mở app N ngày liên tiếp tính streak, milestone 7-ngày nudge sang Shield Score sheet | scan ads/infra |
| ✅U10 | "Shield Score" — Device Health Score badge | **Đã xong** — xác nhận qua code hiện tại (`feat/shield/{ShieldScoreProvider,ShieldScoreCalculator,ShieldScoreBottomSheet}.kt`, badge cạnh icon VIP toolbar, test `shieldScoreBadgeOpensBottomSheetWithScoreAndStreak` trong `ActHostSmokeTest`), doc này trước đó chưa gắn nhãn dù đã ship. Gộp RAM/Storage/Battery thành 1 điểm 0-100 | scan ads/infra |
| ⏸️U11 | VIP "gift a day" share loop | VIP user generate mã 1 lần tặng bạn +1 ngày VIP (giới hạn 1 mã/ngày) → chia sẻ qua Intent.ACTION_SEND có sẵn. Tạo viral loop nhẹ cho app read-only vốn không có social feature | scan ads/infra |
| | **Deferred lần 2 (2026-09-02)** — hỏi lại user sau khi hết việc trong epic-03/quick_win, user chọn "chưa, để sau" lần nữa. Không phải vấn đề kỹ thuật — vẫn là ý tưởng hợp lệ, revisit khi được yêu cầu lại. | |
| ✅U13 | Widget "Điểm sức khoẻ máy" (Shield Score) trên màn hình chính | **Đã xong (2026-09-02)** — package mới `feat/shieldwidget/{ShieldScoreWidgetProvider,ShieldScoreWidgetUpdateWorker}.kt`, đúng khuôn `feat/ramwidget/` (AppWidgetProvider + WorkManager periodic 15 phút, không Hilt vì `DataProviderRam`/`BatteryStatusProvider` constructor thuần framework). Nguồn điểm: `ShieldScoreProvider`/`ShieldScoreCalculator` có sẵn (U10), không có logic tính điểm mới. Layout `widget_shield_score.xml` tái dùng `@drawable/widget_ram_background`. Màu số điểm theo 3 ngưỡng y hệt `ShieldScoreBottomSheet`'s `scoreColor()` (xanh ≥80/cam ≥50/đỏ <50) — đặt bằng `Int` literal thay vì gọi `android.graphics.Color.parseColor()` runtime vì hàm đó trả về 0 dưới cấu hình test `unitTests.isReturnDefaultValues = true` của project, sẽ làm gãy unit test nếu gọi thật. Tap widget mở thẳng `ShieldScoreBottomSheet` qua extra `EXTRA_OPEN_SHIELD_SCORE` trên Intent, xử lý trong `ActHost.onCreate()` (`maybeOpenShieldScoreFromWidget()`) — cơ chế mới, chưa có tiền lệ trong `RamWidgetProvider` (widget đó chỉ mở app, không mở sâu 1 sheet cụ thể). **Edge case phát hiện lúc verify thật, chấp nhận không sửa**: nếu app cài mới hoàn toàn VÀ user tap widget trước khi từng mở app 1 lần, `maybeShowFirstLaunchLanguagePicker()` (chạy async, độc lập) có thể hiện sheet chọn ngôn ngữ đè lên sheet Shield Score vừa mở — race hiếm gặp (cần cài mới + thêm widget + tap widget mà chưa từng mở app), không sửa vì effort/rủi ro của fix (đồng bộ 2 luồng async) không tương xứng với tần suất xảy ra thật. **Verify**: `ShieldScoreWidgetProviderTest` (3 test: schedule/cancel WorkManager, ngưỡng màu đúng 3 dải), build/lint xanh, verify thật trên máy OPPO CPH1989 thật (đặt widget qua UI launcher thật — không phải giả lập, `adb shell cmd appwidget` không hỗ trợ trên máy này) — widget hiện đúng điểm số + màu, tap mở đúng sheet "Điểm sức khoẻ thiết bị" khớp số liệu, đã xoá widget khỏi máy sau khi verify xong (máy cá nhân thật của user) | Claude CLI |
| ✅U14 | Ảnh "thẻ thông tin máy" để chia sẻ (Device ID Card) | **Đã xong (2026-09-02)** — package mới `feat/devicecard/{DeviceCardData,DeviceCardProvider,DeviceCardRenderer,DeviceCardExporter}.kt`. `DeviceCardProvider` gom dữ liệu từ các `DataProvider*`/`ShieldScoreProvider` có sẵn (không đọc hệ thống mới). `DeviceCardRenderer` vẽ trực tiếp bằng `Canvas`/`Paint`/`StaticLayout` (không Compose `captureToImage()`, đúng plan ban đầu) ra `Bitmap` cố định 1080×1350 — panel trắng bo góc trên nền `@color/primary`, badge tròn Shield Score góc trên phải (3 màu theo ngưỡng y hệt `ShieldScoreWidgetProvider.scoreColor()`, dùng `Int` literal không gọi `Color.parseColor()` vì lý do unit test stub y hệt U13), tên máy + chip wrap qua `StaticLayout`, 5 dòng label/value (Số nhân/RAM/Bộ nhớ/Màn hình/Android), footer. `DeviceCardExporter` lưu PNG vào `context.cacheDir`, chia sẻ qua `FileProvider` có sẵn (`file_paths.xml`'s `apk_cache`) + `ACTION_SEND` + `FLAG_ACTIVITY_NEW_TASK` (bắt buộc vì Hilt `@ApplicationContext`, không phải Activity context). **Entry point**: thêm `Format.IMAGE` vào `SystemInfoExporter.Format` enum (dùng chung, không tạo enum riêng), thêm hàng thứ 3 trong `ExportFormatBottomSheet.kt`'s picker sẵn có (TEXT/JSON/IMAGE), `ActHost.registerExportFormatResult()` branch gọi `DeviceCardExporter` khi `format == Format.IMAGE` thay vì `SystemInfoExporter`. **Verify**: `DeviceCardDataTest` (3 test unit cho `formatGb()`), `DeviceCardRendererTest` (4 test androidTest — kích thước bitmap đúng, có vẽ nội dung thật không phải canvas trắng, không crash khi score null/tên máy quá dài) pass trên cả máy thật OPPO CPH1989 và emulator, build/lint xanh, full `ActHostSmokeTest` 22/22 pass trên máy thật (1 lần đầu fail `sensorTestFabRunsThroughAllStepsToADoneScreen` do timeout Compose animation — xác nhận flaky, pass lại ngay khi rerun riêng, không liên quan gì tới thay đổi U14). Verify tay trên máy thật: bấm icon share → chọn "Hình ảnh (chia sẻ)" → share sheet Android hiện đúng ảnh PNG (Facebook hiện thêm option "Đặt làm ảnh đại diện" — dấu hiệu content là ảnh thật, không phải text) → pull file PNG từ cache, ảnh hiển thị đúng thông số máy thật (CPH1989, MediaTek MT6771V/WT, 8 nhân, RAM 7.5GB, bộ nhớ 107.7GB, 1080×2208·60Hz, Android 11, Shield Score 60 màu cam đúng dải). Dịch đủ 6 locale | Claude CLI |
| 📋U15 | Benchmark GPU (điểm hiệu năng đồ hoạ) | **CHỈ LÊN PLAN, CHƯA CODE (2026-09-02)** — hoàn thiện bộ 3 benchmark cùng họ với CPU (Throttle Test, `feat/throttle/`) và Storage (`feat/storagebench/`), còn thiếu GPU. **Đây là ý phức tạp/rủi ro nhất trong 4 ý — effort L (~2-3 ngày), risk trung bình-cao**, khác hẳn U13/U14/U16 vốn tái dùng gần hết hạ tầng có sẵn. **Tech đề xuất**: cần 1 context vẽ đồ hoạ thật để đo — `GLSurfaceView` + `GLSurfaceView.Renderer` tuỳ chỉnh (OpenGL ES, KHÔNG đề xuất Vulkan vì phức tạp hơn nhiều không tương xứng effort), vẽ 1 workload nặng lặp lại (fragment shader tính toán nhiều, hoặc vẽ nhiều tam giác/hình liên tục) trong `onDrawFrame()`, đo số khung hình vẽ được mỗi giây (FPS) trong khoảng thời gian cố định (giống 30s của Throttle) → điểm số. Tái dùng đúng ngưỡng an toàn nhiệt `ThrottleFingerprint.SAFETY_ABORT_TEMP_C` (43°C) và pattern "so với lần chạy trước" của `ThrottleResultPrefs`/`StorageBenchResultPrefs` thay vì bảng điểm cứng theo GPU (không có dữ liệu hiệu chuẩn thật). **Việc cần làm**: package mới `feat/gpubench/` — `GpuBenchmark.kt` (pure fps-scoring math, test được không cần GL thật), `GpuBenchmarkRunner.kt` (`GLSurfaceView.Renderer`, cần xử lý đúng vòng đời `onPause`/`onResume`/context bị mất khi app background — khác các benchmark khác vì đụng tới GPU driver thật), `GpuBenchResultPrefs.kt`, `FrmGpuBench`/`VMGpuBench`/`GpuBenchScreen` theo khuôn `feat/storagebench`. **Rủi ro cần lường trước khi bắt tay code**: (1) GL context/driver quirk khác nhau theo hãng GPU (Mali/Adreno/PowerVR/Xclipse) — 2 máy thật hiện có (TECNO, Galaxy S24 Ultra) không đủ đại diện, cần cẩn trọng khi tuyên bố "đã test kỹ"; (2) GPU stress có thể làm máy nóng nhanh hơn CPU stress test hiện tại, cần theo dõi sát ngưỡng an toàn; (3) `GLSurfaceView` cần 1 View thật gắn vào cây layout (không ẩn hoàn toàn được như benchmark CPU/Storage chạy nền thuần coroutine) — ảnh hưởng cách dựng UI. **Khuyến nghị**: làm sau cùng trong 4 ý, sau khi có thêm máy thật khác hãng GPU để test | Claude CLI |
| ✅U16 | Benchmark RAM (tốc độ đọc/ghi bộ nhớ) | **Đã xong (2026-09-02)** — package mới `feat/rambench/{RamBenchmark,RamBenchmarkRunner,RamBenchResultPrefs,VMRamBench,RamBenchScreen,FrmRamBench}.kt`, đúng khuôn `feat/storagebench/` nhưng thao tác trên `ByteArray` 32MB (`System.arraycopy` ghi/đọc lặp lại 500ms/phase, không phải fixed-size-1-pass vì memory copy quá nhanh để đo chính xác 1 lần) thay vì file cache. **An toàn thêm so với Storage Benchmark**: kiểm `Runtime.getRuntime().maxMemory()` trước khi cấp phát — cần ≥4x buffer size còn trống mới chạy, nếu không đủ thì abort với lý do riêng (`AbortReason.INSUFFICIENT_MEMORY`, khác `OVERHEAT`) thay vì liều cấp phát rồi crash `OutOfMemoryError`. Tab mới vị trí 17 trong ViewPager Hardware (`AdtInfoContainerState.kt`). **Verify**: `RamBenchmarkTest` (5 test: mbPerSec, ngưỡng nhiệt, 2 test cho `hasEnoughMemory`), `RamBenchScreenTest` (widget, 5 test — render trực tiếp không qua Activity, **chứng minh được** dù Storage Benchmark/Dashboard trước đó bỏ qua kiểu test này vì lý do khác: harness NPE của 2 tab đó chỉ xảy ra khi `ActHostSmokeTest` điều hướng qua ViewPager bằng `composeRule` gắn Activity, không phải khi render Screen composable độc lập) — full suite 22/22 pass trên máy OPPO CPH1989 thật, chạy tay 2 lần benchmark thật trên máy (Ghi 4601.8 MB/s / Đọc 4088.3 MB/s lần 1, xác nhận "Lần trước" hiện đúng ở lần 2), không crash/OOM | Claude CLI |
| | **Thứ tự đề xuất nếu làm cả 4 (2026-09-02)**: U13 → U16 → U14 → U15. U13 + U16 + U14 đã xong. Còn lại: U15. Lý do: U15 để cuối vì effort/risk cao nhất và nên có thêm máy test khác hãng GPU trước khi bắt đầu. | |

---

## ✅ Quyết định đã chốt (2026-08-28)

**Làm cả 3 hướng, kết hợp**: U01 + U02 + (U09+U10). Thứ tự đề xuất theo effort tăng dần, tận dụng lại infra của bước trước cho bước sau:

1. **U09 + U10 trước** (VIP streak + Shield Score) — effort thấp nhất, tái dùng VIP infra + data provider có sẵn 100%, ship được trong 1 sprint nhỏ ngay sau khi Epic 1+2 ổn định. Tạo baseline retention/monetization trước khi đầu tư feature nặng.
2. **U02 tiếp theo** (Throttling Fingerprint) — effort trung bình, tái dùng `ObservableCpuData` polling infra đã có (sau khi tối ưu ở T2.18). Có thể xuất "Hardware Health Card" chia sẻ được — vòng lặp viral nhẹ giống U11.
3. **U01 sau cùng** (Device Truth Score / Silicon Detective) — effort lớn nhất, cần nghiên cứu database chipset kỹ, nhưng giá trị kinh doanh dài hạn cao nhất và có thể tái dùng phần data thu thập được từ U02 (đọc thanh ghi/topology).
4. **U12 (AI Readiness Score) cân nhắc chèn song song U01** vì dùng chung nền `libcpuinfo` flags chưa khai thác — nếu build F10 (Epic 3) trước, U12 gần như free add-on.

**Không phải đợi hết Epic 1+2 mới bắt đầu U09/U10** — 2 ý tưởng này không đụng vùng code đang tech-debt (Applications/Storage), có thể xen kẽ ngay khi rảnh 1-2 ngày giữa các sprint bugfix.
